
  <%= link_to "Log out", logout_path, method: :post %>
<section>
TODO SPOTLIGHT
</section>

<h2>Top Albums</h2>
<% albums = Work.where(category: "album") %>
     <% albums.sample(10).each do |album| %>
      <td>Title: <%= link_to album.title, work_path(album.id) %></td>
      <li>Created by: <%= album.artist %></li>
      <li>Published: <%= album.published %>
      <br>
    <% end %>
    </section>

    <section>
<h2>Top Books</h2>
<% books = Work.where(category: "book") %>
     <% books.sample(10).each do |book| %>
      <td>Title: <%= link_to book.title, work_path(book.id) %></td>
      <li>Created by: <%= book.artist %></li>
      <li>Published: <%= book.published %>
      <br>
    <% end %>
    </section>

    <section>
<h2>Top Movies</h2>
<% movies = Work.where(category: "movie") %>
     <% movies.sample(10).each do |movie| %>
      <td>Title: <%= link_to movie.title, work_path(movie.id) %></td>
      <li>Created by: <%= movie.artist %></li>
      <li>Description: <%= movie.published %></li>
      <br>
    <% end %>
    </section>

